{{- if .Values.epicsConfiguration.channelfinder -}}

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "{{ .Values.beamline}}-{{ .Values.channelfinder }}"
  namespace: argocd
  labels:
    rootapp: {{ .Chart.Name }}
    rootappver: {{ .Chart.Version }}
    beamline: {{ .Values.beamline | quote }}
        revision: {{.Values.targetRevision}}

spec:
  project: default
  source:
    repoURL: 'https://baltig.infn.it/epics-containers/channelfinder-chart.git'
    path: .
    targetRevision:  {{.Values.targetRevision}}
    helm:
      values: |
          beamline: {{ .Values.beamline | quote }}
          replicaCount: {{ .Values.consoleReplica }}  
          configCA:
            existingConfigMap: {{ .Values.configCA.configName | quote}}
            address_list: {{ .Values.configCA.gatewayName }} ## override with gateway
          elasticsearch:
            network:
              host: {{ .Values.backend.elasticsearch.host}}.{{ .Values.backend.namespace}}.svc.cluster.local
          aa:
            urls: "{'default': 'http://{{- include "archiver-url" . -}}:17665'}"
            enabled: true
            pva: true

          ingress:
            enabled: true
            hosts:
              - host: '{{- include "channelfinder-url" . }}'
                paths:
                  - path: /
                    pathType: ImplementationSpecific
                    port: 80
    
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: {{ .Values.namespace | quote }}
  syncPolicy:
    automated:
      prune: true  # Optional: Automatically remove resources not specified in Helm chart
      selfHeal: true
{{-end -}}