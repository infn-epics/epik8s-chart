{{- if .Values.epicsConfiguration.services.olog.name }}

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "{{ .Values.beamline}}-{{ .Values.epicsConfiguration.services.olog.name  }}"
  namespace: argocd
  labels:
    rootapp: {{ .Chart.Name }}
    rootappver: {{ .Chart.Version }}
    beamline: {{ .Values.beamline | quote }}
    revision: {{.Values.targetRevision}}

spec:
  project: default
  source:
    repoURL: 'https://baltig.infn.it/epics-containers/phoebus-services-chart.git'
    path: .
    targetRevision: {{.Values.targetRevision}}
    helm:
      values: |
          beamline: {{ .Values.beamline | quote }}
          replicaCount: {{ .Values.consoleReplica }}
          serviceAccountName: {{.Values.serviceAccountName }}
          {{- if .Values.epicsConfiguration.services.olog.securityContext }}
          securityContext:
            {{- toYaml .Values.epicsConfiguration.services.olog.securityContext | nindent 12 }}
          {{- else }}
          securityContext:
            {{- if .Values.openshift }}
            privileged: true
            {{- end }}
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000
          {{- end}}
          phoebusservice: "olog"
          service:
            port: 80
            loglevel: {{.Values.epicsConfiguration.services.olog.loglevel}}
            http:
              enable: true
              port: 8080
          epicsConfiguration:
            name: {{ .Values.epicsConfiguration.name | quote}}
            gateway: '{{- include "gateway-service" . }}' ## override with gateway
          elasticsearch:
            network:
              host: elasticsearch.backend.svc.cluster.local
          mongo:
            host: {{ .Values.backend.mongo.host}}.{{ .Values.backend.namespace}}.svc.cluster.local
            database: "{{.Values.beamline}}-ologAttachments"
          ingress:
            enabled: true
            hosts:
              - host: '{{- include "olog-url" . }}'
                paths:
                  - path: /
                    pathType: ImplementationSpecific

    
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: {{ .Values.namespace | quote }}
  syncPolicy:
    {{- toYaml .Values.syncPolicy | nindent 10 }}
{{- end -}}